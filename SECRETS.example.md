# HabitBloom Secrets

这个文件只放占位示例。复制为本地 `SECRETS.md` 后再填真实值。

```text
HABITBLOOM_REMOTE_BASE_URL=
HABITBLOOM_REMOTE_DEVICE_KEY=
HABITBLOOM_REMOTE_WRITE_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_WORKER_NAME=habitbloom-widget
```

`SECRETS.md` 已被 `.gitignore` 忽略，不要提交真实密钥。
